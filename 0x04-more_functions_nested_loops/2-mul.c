#include "main.h"
#include <stdio.h>
/**
 *mul - checks whether the given string is upper case
 *@a: the variable
 *@b: another variable
 *Return: the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
